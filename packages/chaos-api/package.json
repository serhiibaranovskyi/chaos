{
  "name": "chaos-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "dev": "pnpm run dev:docker:up",
    "dev:db:migrate": "prisma migrate dev",
    "dev:docker:stop": "docker compose -f ./docker/docker-compose.dev.yml -p chaos-api stop",
    "dev:docker:up": "docker compose -f ./docker/docker-compose.dev.yml -p chaos-api up -d",
    "format": "prettier \"src/**/*.{js,mjs,ts,mts,md,json}\" --write",
    "lint": "eslint \"src/**/*.{js,mjs,ts,mts}\"",
    "test": "./scripts/tests:run.sh",
    "test:db:migrate": "dotenv -e .env.test -- prisma migrate deploy",
    "test:docker:down": "docker compose -f ./docker/docker-compose.test.yml -p chaos-api-test down",
    "test:docker:up": "dotenv -e .env.test -- docker compose -f ./docker/docker-compose.test.yml -p chaos-api-test up -d",
    "test:types": "tsc --noEmit",
    "watch": "tsc --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^4.8.1",
    "fastify": "^4.12.0",
    "prisma": "^4.8.1"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "@jest/globals": "^29.3.1",
    "@types/node": "^18.11.18",
    "dotenv-cli": "^6.0.0",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
